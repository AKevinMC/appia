<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APMojocoya"
        tools:targetApi="31">

        <activity
            android:name=".activities.StartingScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.UserDetailActivity"
            android:exported="false" />

        <activity
            android:name=".activities.NewUserActivity"
            android:exported="false" />

        <activity
            android:name=".activities.UserActivity"
            android:exported="false" />

        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ReadingActivity"/>
        <activity android:name=".activities.YearActivity" />
        <activity android:name=".activities.MonthActivity" />
        <activity android:name=".activities.ReportActivity" />
        <activity android:name=".activities.GastosActivity" />
        <activity android:name=".activities.CobranzaActivity" />
        <activity android:name=".activities.DetalleCobroActivity" />
        <activity android:name=".activities.ConfigTarifaActivity" />
        <activity android:name=".activities.InstitucionActivity" />

        <activity
            android:name=".dialogs.DialogAddMedidor"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.gms.signin.CLIENT_ID"
            android:value="@string/default_web_client_id" />
        <meta-data
            android:name="com.google.firebase.firebase.AuthRegistrar"
            android:value="com.google.firebase.auth.FirebaseAuthRegistrar" />
    </application>

</manifest>